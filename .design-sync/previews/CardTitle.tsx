import { Card, CardHeader, CardTitle } from "@aipanel/ui";

export function InCard() {
  return (
    <Card className="max-w-sm">
      <CardHeader>
        <CardTitle>Execution plan</CardTitle>
      </CardHeader>
    </Card>
  );
}
